# AI 协作总规则

本文件适用于所有参与 Narraverse2.0 的 AI。

## 1. 唯一代码源

- 正式代码仓库：`llys-p/Narraverse2.0`
- 协同仓库：`llys-p/Narraverse-Orchestrator`
- 协同仓库禁止保存 Narraverse2.0 的完整源码副本。

## 2. 角色分工

### Workbuddy
主力执行者之一，偏本地工程执行。优先承担：
- 大规模只读调查；
- 本地代码和文档梳理；
- 普通功能实现；
- 测试、构建、回归；
- 环境操作；
- 长时间连续执行；
- 小中型重构；
- 任务记录和 HANDOFF。

### 豆包
主力执行者之一。当前额度充足，甚至可能高于 Workbuddy，因此不得再把豆包默认限制为“少量第二意见”。但豆包存在 **5 小时额度窗口/限制**，任务安排要考虑这个时间窗口。

优先承担：
- 大规模代码/文档阅读与研究；
- 架构分析和系统对照；
- 需求与产品流程梳理；
- 重构方案设计；
- 研究报告和文档整理；
- 重要结论复核；
- 在具备所需仓库或执行权限时承担普通实现、调查和验证。

给豆包的任务应尽量满足：
- 范围清晰；
- 输入一次给全；
- 预期能在同一个 5 小时额度窗口内形成完整产出；
- 尽量减少中途反复重新解释上下文；
- 如果任务可能跨越窗口，必须先落盘中间结论和 HANDOFF，方便下一次继续或交给 Workbuddy。

豆包和 Workbuddy 之间按“谁更适合当前任务、谁已经拥有更多上下文、是否需要本地工程环境、是否适合在豆包的 5 小时窗口内完成”分配，不按过去的额度限制分配。

### Codex
额度较少，只在高价值场景使用：
- 核心架构落地；
- 跨模块复杂重构；
- 高难度 bug；
- 并发、状态一致性、迁移等高风险代码；
- 豆包或 Workbuddy 已完成充分调查，但实现仍明显需要更强代码执行能力时。

不要让 Codex 做机械搜索、全文阅读、普通格式整理或重复测试。

## 3. 开工规则

任何 AI 在修改正式代码前，必须先读：
1. 本文件；
2. `docs/MASTER_PLAN.md`；
3. 当前 TASK 文件；
4. 与任务相关的最近 HANDOFF。

没有 TASK，不改代码。

## 4. 不允许的行为

- 不得因为“看起来应该这样”就假设现有实现。
- 不得同时让多个 AI 修改相同文件范围。
- 不得顺手大改任务范围外的架构。
- 不得为了技术整洁直接推倒仍有价值的功能。
- 不得把调试信息、内部诊断直接当作最终用户 UI。
- 不得把游戏模式、叙界文字冒险、自由沙盒默认视为必须永久独立的三套系统。
- 调查当前 Narraverse2.0 时，必须先确认仓库、branch、commit SHA 和工作目录；不得把旧版 `E:\Narraverse` 当成 Narraverse2.0 当前事实源。

## 5. 任务结束必须交接

每次任务结束必须说明：
- 做了什么；
- 修改了什么；
- 验证了什么；
- 还没做什么；
- 风险/疑点；
- 下一步建议；
- **下一步最适合交给 Workbuddy / 豆包 / Codex 中的谁，以及原因。**

如果下一步需要用户中转，必须给出一段可以直接复制的提示词，不能只写“请继续”。

如果执行者是豆包，并且任务可能受 5 小时额度窗口影响，HANDOFF 还必须额外写清：
- 当前完成到哪一步；
- 哪些中间材料已经落盘；
- 下一窗口可以直接从哪里继续；
- 如果改由 Workbuddy 接手，需要读取哪些文件即可避免重新研究。

## 6. 当前产品原则

目标不是保住旧模块编号，而是形成统一链路：

`导入 → 总资料库 → 世界资料库 → 写作/运行 → 状态/事实 → 审核采纳`

重点验证：
- 写作保持独立工作方式，但共享同一世界资料；
- 游戏模式、叙界文字冒险、自由沙盒是否可以共用同一个运行时核心；
- 故事导演、叙事风格、状态系统、TRPG 检定、事件包等是否可以成为运行预设的可组合组件。

## 7. 第一原则

先查清楚，再改。

当前第一阶段以建立真实 SYSTEM MAP 为主，不允许因为架构讨论兴奋过头就开始大拆房子。人类已经很擅长在没看图纸前砸墙了，AI 没必要跟着学。
